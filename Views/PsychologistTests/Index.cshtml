@model IEnumerable<Sofia.Web.Models.Test>

@{
    ViewData["Title"] = "Мои тесты";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>🛠️ Мои тесты</h2>
    <div class="d-flex gap-2 align-items-center">
        <a href="/psychologist/tests/create" class="btn btn-primary btn-sm">Создать тест</a>
    </div>
</div>

<div class="row g-4">
    @if (Model != null && Model.Any())
    {
        @foreach (var t in Model)
        {
            <div class="col-md-6">
                <div class="card h-100">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@t.Name</h5>
                        <p class="text-muted small mb-3">@t.Description</p>
                        <div class="mt-auto d-flex gap-2">
                            <a class="btn btn-outline-secondary btn-sm" href="/tests/take/@t.Id">Просмотреть</a>
                            <button class="btn btn-danger btn-sm" onclick="deleteTest(@t.Id)">Удалить</button>
                        </div>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <div class="col-12">
            <div class="text-center py-5">
                <div style="font-size:48px;">🛠</div>
                <h4 class="text-muted">У вас ещё нет созданных тестов</h4>
                <p class="text-muted">Создайте первый тест, чтобы предложить его клиентам.</p>
            </div>
        </div>
    }
</div>

<script>
function deleteTest(id){
    if (!confirm('Удалить тест?')) return;
    fetch('/psychologist/tests/delete/' + id, { method: 'POST'})
        .then(r => r.json()).then(j => {
            if (j.success) location.reload();
            else alert(j.message || 'Ошибка');
        });
}
</script>